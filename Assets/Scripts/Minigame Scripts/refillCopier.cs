using UnityEngine;

public class refillCopier : Interactable
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        gameName = "Refill the Copier";
        gameObjective = new string[] { "Find Ink", "place Ink in copier" };
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
