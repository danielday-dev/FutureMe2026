using UnityEngine;

public class smallTalk : minigameTemplate
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        gameName = "Drink Water";
        gameObjective = new string[] { "Grab some water", "Listen to Coworkers Story", "Leave" };
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
